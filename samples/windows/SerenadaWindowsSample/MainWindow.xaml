<Window
    x:Class="Serenada.Windows.Sample.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Serenada Sample">

    <Grid RowSpacing="12" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Serenada Sample"
                   FontSize="24" FontWeight="Bold"
                   Grid.Row="0" Margin="0,0,0,16" />

        <!-- URL-first join -->
        <TextBox x:Name="UrlInput"
                 PlaceholderText="Paste a call URL..."
                 Grid.Row="1" Padding="10,8" />

        <Button x:Name="JoinUrlButton"
                Content="Join by URL"
                Grid.Row="2"
                Click="OnJoinUrlClick" />

        <!-- Create room then join -->
        <Button x:Name="CreateRoomButton"
                Content="Create Room and Join"
                Grid.Row="3"
                Click="OnCreateRoomClick" />

        <!-- Provider mode demo -->
        <Button x:Name="ProviderDemoButton"
                Content="Provider Mode Demo"
                Grid.Row="4"
                VerticalAlignment="Bottom"
                Click="OnProviderDemoClick" />
    </Grid>
</Window>
